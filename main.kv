<Trainer>:
    id: trainer
    display: entry
    orientation: 'vertical'
    Label:
        text: trainer.text
    TextInput:
        id: entry
        on_text_validate: trainer.check(entry.text); self.focus=True
        multiline: False
        focus: True
    Button:
        text: "Enter"
        on_press: trainer.check(entry.text)

<TAPopup>:
    title:"Wrong"
    Button:
        text: "Try again"
        on_release: root.dismiss()
<ValueErrorPopUp>
    title:"Wrong"
    Button:
        text: "not a number"
        on_release: root.dismiss()